{
  "name": "auralai-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AuralAI companion + admin + guide UI — Preact build for device/server/static/app/.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
